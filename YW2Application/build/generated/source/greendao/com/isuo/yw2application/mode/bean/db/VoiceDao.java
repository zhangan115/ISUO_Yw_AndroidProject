package com.isuo.yw2application.mode.bean.db;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "file_voice".
*/
public class VoiceDao extends AbstractDao<Voice, Long> {

    public static final String TABLENAME = "file_voice";

    /**
     * Properties of entity Voice.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property _id = new Property(0, Long.class, "_id", true, "_id");
        public final static Property SaveTime = new Property(1, Long.class, "saveTime", false, "SAVE_TIME");
        public final static Property VoiceLocal = new Property(2, String.class, "voiceLocal", false, "VOICE_LOCAL");
        public final static Property BackUrl = new Property(3, String.class, "backUrl", false, "BACK_URL");
        public final static Property MContent = new Property(4, String.class, "mContent", false, "M_CONTENT");
        public final static Property IsUpload = new Property(5, boolean.class, "isUpload", false, "IS_UPLOAD");
        public final static Property WorkType = new Property(6, int.class, "workType", false, "WORK_TYPE");
        public final static Property ItemId = new Property(7, long.class, "itemId", false, "ITEM_ID");
        public final static Property CurrentUserId = new Property(8, long.class, "currentUserId", false, "CURRENT_USER_ID");
        public final static Property VoiceTime = new Property(9, String.class, "voiceTime", false, "VOICE_TIME");
    }


    public VoiceDao(DaoConfig config) {
        super(config);
    }
    
    public VoiceDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"file_voice\" (" + //
                "\"_id\" INTEGER PRIMARY KEY AUTOINCREMENT ," + // 0: _id
                "\"SAVE_TIME\" INTEGER," + // 1: saveTime
                "\"VOICE_LOCAL\" TEXT," + // 2: voiceLocal
                "\"BACK_URL\" TEXT," + // 3: backUrl
                "\"M_CONTENT\" TEXT," + // 4: mContent
                "\"IS_UPLOAD\" INTEGER NOT NULL ," + // 5: isUpload
                "\"WORK_TYPE\" INTEGER NOT NULL ," + // 6: workType
                "\"ITEM_ID\" INTEGER NOT NULL ," + // 7: itemId
                "\"CURRENT_USER_ID\" INTEGER NOT NULL ," + // 8: currentUserId
                "\"VOICE_TIME\" TEXT);"); // 9: voiceTime
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"file_voice\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, Voice entity) {
        stmt.clearBindings();
 
        Long _id = entity.get_id();
        if (_id != null) {
            stmt.bindLong(1, _id);
        }
 
        Long saveTime = entity.getSaveTime();
        if (saveTime != null) {
            stmt.bindLong(2, saveTime);
        }
 
        String voiceLocal = entity.getVoiceLocal();
        if (voiceLocal != null) {
            stmt.bindString(3, voiceLocal);
        }
 
        String backUrl = entity.getBackUrl();
        if (backUrl != null) {
            stmt.bindString(4, backUrl);
        }
 
        String mContent = entity.getMContent();
        if (mContent != null) {
            stmt.bindString(5, mContent);
        }
        stmt.bindLong(6, entity.getIsUpload() ? 1L: 0L);
        stmt.bindLong(7, entity.getWorkType());
        stmt.bindLong(8, entity.getItemId());
        stmt.bindLong(9, entity.getCurrentUserId());
 
        String voiceTime = entity.getVoiceTime();
        if (voiceTime != null) {
            stmt.bindString(10, voiceTime);
        }
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, Voice entity) {
        stmt.clearBindings();
 
        Long _id = entity.get_id();
        if (_id != null) {
            stmt.bindLong(1, _id);
        }
 
        Long saveTime = entity.getSaveTime();
        if (saveTime != null) {
            stmt.bindLong(2, saveTime);
        }
 
        String voiceLocal = entity.getVoiceLocal();
        if (voiceLocal != null) {
            stmt.bindString(3, voiceLocal);
        }
 
        String backUrl = entity.getBackUrl();
        if (backUrl != null) {
            stmt.bindString(4, backUrl);
        }
 
        String mContent = entity.getMContent();
        if (mContent != null) {
            stmt.bindString(5, mContent);
        }
        stmt.bindLong(6, entity.getIsUpload() ? 1L: 0L);
        stmt.bindLong(7, entity.getWorkType());
        stmt.bindLong(8, entity.getItemId());
        stmt.bindLong(9, entity.getCurrentUserId());
 
        String voiceTime = entity.getVoiceTime();
        if (voiceTime != null) {
            stmt.bindString(10, voiceTime);
        }
    }

    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    @Override
    public Voice readEntity(Cursor cursor, int offset) {
        Voice entity = new Voice( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // _id
            cursor.isNull(offset + 1) ? null : cursor.getLong(offset + 1), // saveTime
            cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2), // voiceLocal
            cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3), // backUrl
            cursor.isNull(offset + 4) ? null : cursor.getString(offset + 4), // mContent
            cursor.getShort(offset + 5) != 0, // isUpload
            cursor.getInt(offset + 6), // workType
            cursor.getLong(offset + 7), // itemId
            cursor.getLong(offset + 8), // currentUserId
            cursor.isNull(offset + 9) ? null : cursor.getString(offset + 9) // voiceTime
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, Voice entity, int offset) {
        entity.set_id(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setSaveTime(cursor.isNull(offset + 1) ? null : cursor.getLong(offset + 1));
        entity.setVoiceLocal(cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2));
        entity.setBackUrl(cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3));
        entity.setMContent(cursor.isNull(offset + 4) ? null : cursor.getString(offset + 4));
        entity.setIsUpload(cursor.getShort(offset + 5) != 0);
        entity.setWorkType(cursor.getInt(offset + 6));
        entity.setItemId(cursor.getLong(offset + 7));
        entity.setCurrentUserId(cursor.getLong(offset + 8));
        entity.setVoiceTime(cursor.isNull(offset + 9) ? null : cursor.getString(offset + 9));
     }
    
    @Override
    protected final Long updateKeyAfterInsert(Voice entity, long rowId) {
        entity.set_id(rowId);
        return rowId;
    }
    
    @Override
    public Long getKey(Voice entity) {
        if(entity != null) {
            return entity.get_id();
        } else {
            return null;
        }
    }

    @Override
    public boolean hasKey(Voice entity) {
        return entity.get_id() != null;
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
